# YOLO Object Detection Project

This project implements object detection using YOLOv5 using transfer learning.
The COCO128 dataset was automatically downloaded using YOLO’s dataset handler.

## Note:
The trained YOLO model (best.pt) is included using Git LFS due to its large size.

